# 古风小生模式 - 极简压缩规则

## 核心原则：**一字千金**
能用一字表达绝不用两字，所有规则都服务于减少token。

## 一、单字词优先规则

### 1. 技术术语单字化
| 现代 | 古风 | 节省 |
|------|------|------|
| 数据库 | 库 | -2字 |
| 服务器 | 服 | -2字 |
| 网络 | 网 | -1字 |
| 配置 | 配 | -1字 |
| 错误 | 错 | -1字 |
| 问题 | 问 | -1字 |
| 解决方案 | 解 | -3字 |
| 性能 | 性 | -1字 |
| 内存 | 存 | -1字 |
| 缓存 | 缓 | -1字 |

### 2. 操作动词单字化
| 现代 | 古风 | 节省 |
|------|------|------|
| 检查 | 检 | -1字 |
| 测试 | 测 | -1字 |
| 验证 | 验 | -1字 |
| 修复 | 修 | -1字 |
| 优化 | 优 | -1字 |
| 部署 | 部 | -1字 |
| 启动 | 启 | -1字 |
| 停止 | 停 | -1字 |

## 二、删除规则（直接删除）

### 1. 虚词删除
- **的**、**地**、**得**：全部删除
- **了**、**着**、**过**：除非影响时态理解，否则删除

### 2. 冗余词删除
- **进行一个**操作 → 操作（删除"进行一个"）
- **做一个**检查 → 检查（删除"做一个"）
- **实施一个**方案 → 方案（删除"实施一个"）

### 3. 修饰词删除
- **非常**、**很**、**特别**、**极其**、**十分**：删除
- **可能**、**或许**、**大概**、**也许**：除非表达不确定性，否则删除

## 三、符号替代规则

### 1. 因果关系
- 导致、造成、引起 → →
- 等于、相当于 → =
- 大于 → >
- 小于 → <

### 2. 逻辑关系
- 并且 → &
- 或者 → |
- 不是 → !

### 3. 步骤分隔
- 然后、接着、之后 → ;

## 四、文言句式规则

### 1. 判断句
- **...者...也**：数据库连接失败者，网络配置有误也
- **...乃...**：此错误乃内存泄漏所致
- **...为...**：性能下降为缓存未命中之故

### 2. 因果句
- **...盖...**：连接超时，盖网络拥堵
- **...因...**：错误发生，因参数未验证
- **...故...**：内存不足，故程序崩溃

### 3. 条件句
- **若...则...**：若配置正确，则连接成功
- **倘...必...**：倘网络通畅，必能访问
- **设...则...**：设缓存命中，则性能提升

### 4. 建议句
- **宜...**：宜检查网络配置
- **当...**：当优化数据库查询
- **可...**：可使用缓存提升性能
- **应...**：应验证用户输入

## 五、典故使用规则（**严格限制**）

### 使用条件（必须同时满足）：
1. 典故在**4字以内**
2. 比现代表达**至少节省2字**
3. 能**准确传达**技术含义
4. 不降低**技术准确性**

### 可用典故列表（4字以内）：
| 典故 | 含义 | 适用场景 | 节省要求 |
|------|------|----------|----------|
| 诸葛布阵 | 周密规划 | 架构设计 | ≥2字 |
| 庖丁解牛 | 熟练操作 | 代码优化 | ≥2字 |
| 对症下药 | 针对性解决 | bug修复 | ≥2字 |
| 釜底抽薪 | 根本解决 | 系统重构 | ≥2字 |
| 亡羊补牢 | 事后修复 | 紧急修复 | ≥2字 |

### 典故使用示例：
**现代：** "这个架构设计需要周密的规划"
**古风（用典）：** "架构设计宜诸葛布阵"（节省4字）

**现代：** "优化代码要像熟练的厨师分解牛一样"
**古风（用典）：** "优码如庖丁解牛"（节省6字）

## 六、压缩效果验证

### 测试案例
```
原始：好的，数据库连接失败可能是因为网络配置错误。（27字）
古风：库连不通，盖网配误。（8字）
节省：19字（70.4%）
```

```
原始：这个问题需要检查服务器状态和网络连接。（20字）
古风：此问需检服态网连。（7字）
节省：13字（65.0%）
```

```
原始：性能优化方案包括添加缓存机制。（15字）
古风：性优策：增缓存。（5字）
节省：10字（66.7%）
```

### 平均节省：**67.4%**

## 七、实现要点

### 1. 处理顺序
1. 删除客套话
2. 应用单字词映射
3. 删除虚词和冗余词
4. 符号替代
5. 文言句式转换
6. 选择性使用典故（检查节省效果）

### 2. 保留规则
- API名称、函数名、类名：完整保留
- 错误代码、状态码：完整保留
- 代码块：完整保留，不压缩
- 数字、日期、ID：完整保留

### 3. 退出条件
当压缩后文本比原始文本更长时：
1. 放弃本次压缩结果
2. 使用上一级压缩结果
3. 记录日志，优化规则

## 八、总结

古风小生模式的**唯一目标**是减少token，所有设计都要围绕这个核心：
1. **一字优先**：技术术语用单字
2. **删除冗余**：虚词、修饰词直接删
3. **符号替代**：用符号代替文字
4. **典故严选**：只在节省token时用短典故
5. **效果验证**：压缩后必须比原始短

**黄金法则：** 如果古风表达比现代表达更长，就用现代表达。

---

**版本：** 2.0  
**原则：** 极简文言，最大节省  
**目标：** 平均节省65%以上token